# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: installation package

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.8", "3.9", "3.10", "3.11"]
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: installation file-validator in windows
      if: matrix.os == 'windows-latest'
      run: python.exe -m pip install file-validator
    - name: installation file-validator in macos and ubuntu
      if: matrix.os == 'ubuntu-latest' || matrix.os == 'macos-latest'
      run: python -m pip install -i https://test.pypi.org/simple/ file-validator==0.3.1
